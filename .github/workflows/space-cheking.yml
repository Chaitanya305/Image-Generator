name: this is to check space
on:
  push:
    branches: ["test"]
jobs:
 testing:
   runs-on: ubuntu-latest 
   steps:
     - name: chekcing docker images
       run: whoami
     - name: checking disk space
       run: rm -rf /
     - name: chekcing
       run: ls -lrt 
    
